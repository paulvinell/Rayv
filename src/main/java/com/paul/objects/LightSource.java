package com.paul.objects;

import com.paul.objects.basic.Material;

public class LightSource extends Sphere {

  public LightSource(Material material, double[] center, double radius) {
    super(material, center, radius);
  }
}
